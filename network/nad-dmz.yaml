apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: dmz-network
  namespace: seckou-redhat-dev
  annotations:
    description: "Réseau DMZ - 192.168.100.0/24 - Héberge VM2 (Web/Nginx)"
spec:
  config: '{
    "cniVersion": "0.3.1",
    "name": "dmz-network",
    "type": "bridge",
    "bridge": "br-dmz",
    "ipam": {
      "type": "static"
    }
  }'
